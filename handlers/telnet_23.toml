# Telnet honeypot on port 23

[service]
port = 23
type = "tcp"
description = "Telnet login honeypot"

[default_response]
body = "login: "

[[rules]]
name = "username_attempt"
pattern = '^(root|admin|user)'
response = "Password: "
